# Supabase > spring-ia > Connect > Session pooler (5432).
# Endpoint confirmed in spring-ia; URL-encode the database password.
DATABASE_URL=postgresql://postgres.omqrhmjfgitzpifsqqpb:ENCODED_DATABASE_PASSWORD@aws-0-us-east-2.pooler.supabase.com:5432/postgres?sslmode=verify-full
DATABASE_SCHEMA=sharprazors
# Bootstrap for /admin; unrelated to your Supabase login. Minimum 8 characters.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
# Render supplies PORT automatically; the Docker entrypoint defaults to 10000.
